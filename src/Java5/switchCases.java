package Java5;

public class switchCases {
    public static void main(String[] args) {

        // no & || > < == !== only equality reverse if
        // very fast but limited scenarios
        // should match the same data types int same int

        // ToUpperCase  ToLowerCase   not work with char
        // .equalsIgnoreCases  for only compare boolean equals string










    }

    }

